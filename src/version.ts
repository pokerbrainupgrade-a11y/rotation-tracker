/**
 * App version stamped into backup envelopes. Bump alongside package.json —
 * it is diagnostic metadata for a restore, not a compatibility gate. The gate
 * is `schemaVersion`.
 */
export const APP_VERSION = '0.1.0';
